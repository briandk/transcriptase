const saveOptions = {
  filters: [
    {
      extensions: ["txt"],
      name: "text",
    }, // sets default file extension
  ],
  properties: ["createDirectory"],
  title: "Save Your Transcript",
};

export { saveOptions };
