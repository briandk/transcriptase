compile:
  override:
    - npm run build-windows
dependencies:
  pre: #install Wine
    - sudo add-apt-repository --yes ppa:ubuntu-wine/ppa
    - sudo apt-get update
    - sudo apt-get install --yes wine1.7
