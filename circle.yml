version: 2 # CircleCI version 2

machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn

test:
  docker:
    - image: circleci/node:7.4.0
  override:
    - yarn test
