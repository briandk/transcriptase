compile:
  override:
    - npm run build-windows
dependencies:
  pre: #install Wine
    - sudo apt-get update
    - sudo apt-get build-dep --yes udev wine1.6-i386
    # - sudo apt-get install --yes wine1.6
